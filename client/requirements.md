## Packages
framer-motion | Smooth scroll animations and fade-ins for premium feel
react-icons | Additional icon sets if Lucide isn't enough (though Lucide is primary)

## Notes
- Dark mode is default
- "Book Free Demo" opens a dialog with a lead capture form (POST /api/leads)
- Video placeholders will use abstract gradients or subtle patterns
- Font choices: Inter for UI/Body, Outfit for Headings (imported in index.css)
